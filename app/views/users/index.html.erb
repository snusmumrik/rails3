<h1><%=t "activerecord.models.user" %><%=t "views.index"  %></h1>

<table>
  <tr>
    <th><%=t "activerecord.attributes.user.name" %></th>
    <th><%=t "activerecord.attributes.user.username" %></th>
    <th><%=t "activerecord.attributes.user.email" %></th>
    <th><%=t "activerecord.attributes.user.image" %></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

  <% @users.each do |user| %>
    <tr>
      <td><%= user.name %></td>
      <td><%= user.username %></td>
      <td><%= user.email %></td>
      <td>
        <% unless user.images.blank? %>
          <%= image_tag user.images.pop.image.url(:thumb) %>
        <% end %>
      </td>
      <td><%= link_to t("link.show"), user_path(user.username) %></td>
    </tr>
  <% end %>
</table>

<%= paginate @users %>
